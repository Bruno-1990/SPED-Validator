# ──────────────────────────────────────────────
# SPED EFD Audit - Dependencias Python
# ──────────────────────────────────────────────
# Instalar: pip install -r requirements.txt

# API / Backend
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
pydantic>=2.0.0
starlette>=0.27.0

# YAML (rules.yaml)
pyyaml>=6.0

# Indexação de documentos e busca semântica
pdfplumber>=0.10.0
sentence-transformers>=2.2.0
numpy>=1.24.0
torch>=2.0.0
tqdm>=4.65.0

# Testes
pytest>=7.0.0
pytest-cov>=4.0.0
