# ──────────────────────────────────────────────
# SPED EFD Audit - Dependencias Python
# ──────────────────────────────────────────────
# Instalar: pip install -r requirements.txt

# API / Backend
fastapi>=0.100.0,<1.0.0
uvicorn>=0.23.0
python-multipart>=0.0.6
pydantic>=2.0.0,<3.0.0
starlette>=0.27.0

# YAML (rules.yaml + tabelas de referencia)
pyyaml>=6.0

# Conversao de documentos
python-docx>=0.8.0

# Indexacao de documentos e busca semantica
pdfplumber>=0.10.0
sentence-transformers>=2.2.0,<3.0.0
numpy>=1.24.0,<2.0.0
tqdm>=4.65.0

# PyTorch CPU-only (modelo all-MiniLM-L6-v2 nao precisa de GPU)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0,<3.0.0

# Testes
pytest>=7.0.0
pytest-cov>=4.0.0
httpx>=0.25.0
